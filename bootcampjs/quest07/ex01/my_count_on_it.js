/*
**
** QWASAR.IO -- my_count_on_it
**
**
** @param {String[]} param_1
** @return {integer[]}
**
*/


function my_count_on_it(count) {
    let arr = count.map(k => k.length);

    return arr;

};

// console.log(my_count_on_it(["This", "is", "the", "way"]))